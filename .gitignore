# Local-only context files; this repo is public
David Dangerfield Resume.txt
automated-recruiter-response.txt
LinkedIn-Profile.pdf

node_modules/

dist/

.DS_Store

temp/

# Tailored resume variants (npm run resume:variant). Ephemeral and private:
# they show where David is applying, so they're never committed.
applications/
